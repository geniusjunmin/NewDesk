<Window x:Class="NewDesk.MasterPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="主密码" Height="350" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="#F5F5F5">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#4A90E2"/>
        <SolidColorBrush x:Key="CardBackgroundColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextColor" Color="#333333"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="DangerColor" Color="#FF4D4F"/>
        
        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{StaticResource CardBackgroundColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>
    
    <Border Background="#F5F5F5" CornerRadius="12" Margin="8">
        <Grid Margin="32">
            <StackPanel VerticalAlignment="Center">
                <!-- 标题 -->
                <TextBlock Text="🔒 主密码" FontSize="24" FontWeight="Bold" 
                           Foreground="{StaticResource PrimaryColor}" 
                           Margin="0,0,0,32" HorizontalAlignment="Center"/>
                
                <!-- 说明文字 -->
                <TextBlock Name="InstructionTextBlock" 
                           Text="请输入主密码:" 
                           FontSize="15"
                           Margin="0,0,0,12"/>
                
                <!-- 主密码输入 -->
                <PasswordBox Name="MasterPasswordBox" Margin="0,0,0,16"/>
                
                <!-- 确认密码 -->
                <TextBlock Name="ConfirmTextBlock" 
                           Text="确认主密码:" 
                           FontSize="15"
                           Margin="0,16,0,12" 
                           Visibility="Collapsed"/>
                <PasswordBox Name="ConfirmPasswordBox" 
                             Margin="0,0,0,16" 
                             Visibility="Collapsed"/>
                
                <!-- 错误信息 -->
                <TextBlock Name="ErrorTextBlock" 
                           Foreground="{StaticResource DangerColor}" 
                           FontSize="13"
                           Margin="0,0,0,24" 
                           TextWrapping="Wrap"
                           Visibility="Collapsed"/>
                
                <!-- 按钮 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Name="OkButton" Content="确定" Width="100" 
                            Style="{StaticResource ModernButton}"
                            IsDefault="True" Click="OkButton_Click"/>
                    <Button Name="CancelButton" Content="取消" Width="100" 
                            Margin="12,0,0,0"
                            Background="#E0E0E0" Foreground="{StaticResource TextColor}"
                            BorderThickness="0" Padding="20,10" Cursor="Hand"
                            IsCancel="True" Click="CancelButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="6" 
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D0D0D0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
